<Window x:Class="ChatP2P.Client.AddContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Contact" Height="300" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#FF2B2B2B">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Add New Contact" FontSize="18" FontWeight="Bold" 
                   Foreground="White" Margin="0,0,0,20"/>
        
        <Label Grid.Row="1" Content="Peer Name:" Foreground="White"/>
        <TextBox Grid.Row="2" Name="txtPeerName" Margin="0,5,0,15" 
                 Background="#FF404040" Foreground="White" BorderBrush="#FF666666" 
                 Padding="10,8" FontSize="14"/>
        
        <Label Grid.Row="3" Content="Public Key:" Foreground="White"/>
        <TextBox Grid.Row="4" Name="txtPublicKey" Margin="0,5,0,15" 
                 Background="#FF404040" Foreground="White" BorderBrush="#FF666666" 
                 Padding="10,8" FontSize="12" TextWrapping="Wrap" AcceptsReturn="True"/>
        
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="btnCancel" Content="Cancel" Width="80" Height="35" 
                    Margin="0,0,10,0" Background="#FF666666" Foreground="White" 
                    BorderBrush="Transparent" Click="BtnCancel_Click"/>
            <Button Name="btnAdd" Content="Add Contact" Width="100" Height="35" 
                    Background="#FF0D7377" Foreground="White" BorderBrush="Transparent" 
                    Click="BtnAdd_Click"/>
        </StackPanel>
    </Grid>
</Window>